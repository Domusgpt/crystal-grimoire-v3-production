// Add these to your functions/package.json dependencies:

{
  "dependencies": {
    "firebase-functions": "^6.0.0",
    "firebase-admin": "^12.0.0",
    "@google/generative-ai": "^0.21.0"
  }
}

// That's it! Just these 3 dependencies needed.